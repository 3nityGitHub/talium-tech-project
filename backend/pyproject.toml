[project]
name = "talium-backend"
version = "0.1.0"
description = "Talium Tech Care Management — backend API"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
